import React from 'react';
import ReactDOM from 'react-dom';
import { Button } from '@mui/material';

function MuiTest() {
  return (
    dddd
  );
}

ReactDOM.render(<MuiTest />, document.getElementById('root'));
